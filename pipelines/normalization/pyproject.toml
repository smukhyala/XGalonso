[project]
name = "xg-alonso-normalization"
version = "0.1.0"
description = "Bronze to silver: canonical player, team, fixture and gameweek tables."
requires-python = ">=3.12"
dependencies = [
    "polars>=1.20,<2",
    "xg-alonso-contracts",
    "xg-alonso-domain",
    "xg-alonso-storage",
]

[tool.uv.sources]
xg-alonso-contracts = { workspace = true }
xg-alonso-domain = { workspace = true }
xg-alonso-storage = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/xg_alonso"]
